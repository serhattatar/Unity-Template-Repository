/* ==================================================================================
 * 🖥️ UI PANEL (HUD)
 * ==================================================================================
 * Description:   For persistent UI elements (Health Bars, Coin Indicators).
 * Does not enter the History Stack. Does not block input.
 * ==================================================================================
 */

using UI.Core;

public class UIPanel : UIView
{
    // Inherits default Fade Animation.
    // Panels generally remain persistent, so no extra logic is needed here.
}